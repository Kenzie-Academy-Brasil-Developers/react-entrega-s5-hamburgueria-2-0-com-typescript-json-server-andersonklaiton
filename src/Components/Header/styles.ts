import styled from "styled-components";

export const P = styled.p`
  font-size: 28px;
  margin: 10px;
  font-weight: bold;
  color: var(--collorGray600)
`;
export const Label = styled.label`
font-size: 20px;
color: var(--collorSecundary);
`